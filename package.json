{
  "name": "@sebastian-gmbh/paratix-workspace",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r --filter './packages/*' exec tsc --noEmit",
    "lint": "run-p --print-label lint:*",
    "lint:oxlint": "oxlint -c oxlint.config.ts",
    "lint:eslint": "eslint",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "agent:check": "pnpm lint && pnpm format:check && pnpm typecheck && pnpm test"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/sebastian-software/paratix.git"
  },
  "bugs": {
    "url": "https://github.com/sebastian-software/paratix/issues"
  },
  "author": "Sebastian Fastner <s.fastner@sebastian-software.de>",
  "license": "MIT",
  "homepage": "https://paratix.oss.sebastian-software.com",
  "engines": {
    "node": ">=24.0.0"
  },
  "packageManager": "pnpm@10.30.3",
  "pnpm": {
    "onlyBuiltDependencies": [
      "cpu-features",
      "esbuild",
      "ssh2"
    ]
  },
  "devDependencies": {
    "@cspell/dict-de-de": "^4.1.2",
    "eslint": "^10.0.3",
    "eslint-config-setup": "^0.3.3",
    "jiti": "^2.6.1",
    "npm-run-all2": "^8.0.4",
    "oxlint": "^1.55.0",
    "prettier": "^3.8.1"
  }
}
